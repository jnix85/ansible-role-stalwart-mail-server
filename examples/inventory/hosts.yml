---
all:
  children:
    mailservers:
      hosts:
        mail.example.com:
          # IP or resolvable address Ansible connects to.
          ansible_host: 203.0.113.10
          ansible_user: root
          # ansible_ssh_private_key_file: ~/.ssh/id_ed25519
